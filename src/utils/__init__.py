"""
Utility functions for geometry calculations and visualization.

This module provides utilities for:
- Geometry calculations (HandLandmark, distances, smoothing)
- Visualization (drawing landmarks, info display)  
- Configuration management (setting controls/gestures)
- Validation (config validation)

Import specific modules as needed:
    from src.utils.geometry_utils import HandLandmark
    from src.utils.visualizer import draw_hand_landmarks
    from src.utils.config_helper import set_control_enabled
    from src.utils.validator import validate_config
"""

# Note: We don't import everything here to avoid relative import issues
# when modules are imported individually. Import specific modules as needed.
